"use client";
// ThemeSelector is now handled directly in DashboardLayout via useTheme()
// This file kept for backwards compatibility but renders nothing
export default function ThemeSelector() {
  return null;
}
